import React from 'react';
import { CursorBackgroundFollower } from './CursorBackgroundFollower';

export const GlobalBackground: React.FC = () => {
  return <CursorBackgroundFollower />;
};

export default GlobalBackground;
