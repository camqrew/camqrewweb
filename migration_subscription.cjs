const { Client } = require('pg'); const client = new Client('postgresql://postgres.lwvmtjraqvniknstcvpk:Adgjmpu123%40%23@aws-0-ap-south-1.pooler.supabase.com:6543/postgres'); async function run() { await client.connect(); try { await client.query("ALTER TABLE users ADD COLUMN IF NOT EXISTS subscription_tier VARCHAR DEFAULT 'free', ADD COLUMN IF NOT EXISTS subscription_status VARCHAR DEFAULT 'inactive', ADD COLUMN IF NOT EXISTS subscription_end_date TIMESTAMP;"); console.log('Users table updated with subscription columns'); } catch(e) { console.log(e.message); } client.end(); } run();
